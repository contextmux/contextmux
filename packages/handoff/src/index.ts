/**
 * Handoff: moving an unfinished task from one agent to another.
 *
 * Built on the trajectory rather than the transcript, because the thing worth transferring is
 * what has been ruled out — and a transcript buries that under the reasoning that produced it.
 */
export * from './package.js'
export * from './render.js'
